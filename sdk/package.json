{
  "name": "@ekubo/evm-hyper-router-sdk",
  "version": "0.0.3",
  "description": "Calldata generation for Ekubo's gas-efficient and highly configurable HyperRouter smart contract",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "keywords": [
    "ekubo",
    "amm",
    "dex",
    "ethereum",
    "evm",
    "defi",
    "finance",
    "crypto",
    "blockchain",
    "smart-contracts"
  ],
  "scripts": {
    "build": "tsup src/index.ts --dts --minify --tsconfig src/tsconfig.json",
    "test": "vitest",
    "generate-testdata": "wagmi generate 2>/dev/null && ts-node test/generate-testdata.ts",
    "prepublishOnly": "rm -rf dist/ && npm run build"
  },
  "devDependencies": {
    "@wagmi/cli": "^2.3.2",
    "ts-essentials": "^10.1.1",
    "ts-node": "^10.9.2",
    "tsup": "^8.5.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "viem": "^2.33.0"
  }
}